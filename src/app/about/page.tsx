import ProfileCard from '@/components/ProfileCard';

export const metadata = {
	title: 'About',
	description: 'pSpace 소개 페이지입니다.',
};

export default function AboutPage() {
	return (
		<>
			<ProfileCard />
		</>
	);
}
